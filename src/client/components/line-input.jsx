import React, { Component } from 'react';

// TODO: this component
/* provide an input and corresponding formatting controls for the line. pass up options and text with onChange emitter. */

export default class LineInput extends Component {
	constructor(props) {
		super(props);
	}
	render() {
		return <div></div>;
	}
}